import React from "react";
import Layout from "../components/ui/layout";

const RoomType = () => {
    return (
        <Layout>
            {(currency) => (
                <h1>RoomType</h1>
            )
            }
        </Layout>
    )
}

export default RoomType;